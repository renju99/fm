<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- This file can be used to assign default groups to users -->
        <!-- Administrators can manually assign the 'Facilities Focused User' group to users who should have a clean interface -->
        
        <!-- Example: Assign all users with 'facilities' in their email to facilities focused group -->
        <!-- This is commented out by default - uncomment and modify as needed -->
        <!--
        <function model="res.users" name="_assign_facilities_focused_group">
            <value eval="[]"/>
        </function>
        -->
        
        <!-- Note: Users can be assigned to the 'Facilities Focused User' group manually through: -->
        <!-- Settings > Users & Companies > Users > [Select User] > Access Rights tab > Other section -->
        <!-- Check the "Facilities Focused User" checkbox to hide unrelated menus for that user -->
        
    </data>
</odoo>
